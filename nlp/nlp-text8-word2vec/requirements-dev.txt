# Test runner + TestClient transport
pytest==8.3.5
pytest-cov==4.1.0
# starlette 0.27 (used by fastapi 0.104.1) + httpx 0.28 broke TestClient(app);
# pin <0.28 per the [[phase_2a_complete]] lesson.
httpx<0.28

# Lint / type / format
black==23.12.0
flake8==6.1.0
isort==5.13.2

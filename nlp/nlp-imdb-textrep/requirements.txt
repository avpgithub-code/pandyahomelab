# Web layer
fastapi==0.104.1
uvicorn[standard]==0.24.0
pydantic==2.5.0

# NLP + numerics
numpy==1.26.3
pandas==2.0.3
scikit-learn==1.3.2
# L3 token panel: spaCy tokenizer + lemmatizer (en_core_web_sm is installed in the
# Dockerfile), NLTK's PorterStemmer (no corpus download needed)
spacy==3.7.5
nltk==3.8.1
# Reads the IMDB parquet files
pyarrow==15.0.2

# Tracking — pinned to the nlp-mlflow server version (3.11.1). The DL images left
# this unpinned and resolved a newer client (3.15.x) than their server.
mlflow==3.11.1
